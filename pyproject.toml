[project]
name = "revenue-product-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.8",
    "dbt-databricks>=1.11.7",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "sqlfluff>=4.1.0",
]
